import { OverallDatabaseInfo } from '@app/interfaces/interfaces';

export const DATE_FORMAT = 'dd/MM/yyyy';

export const INITIAL_OVERALL_DB_INFO: OverallDatabaseInfo = {
  dateCreated: 'N/A',
  schemaCount: 0,
  totalTableCount: 0,
  viewCount: 0,
  totalColCount: 0,
};
